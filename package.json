{
  "name": "caffeine-usersnap",
  "version": "0.0.1",
  "description": "Easier site annotation tool for Caffeine RDE",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pip3 install Pillow && pip3 install Flask && pip3 install -U flask-cors && pip3 install mss && export FLASK_APP=overlay/functions/process/accept && export FLASK_DEBUG=1 && python3 -m flask run & nodemon app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "jquery": "^3.7.0",
    "nodemon": "^3.0.1"
  }
}
